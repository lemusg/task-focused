VITE_BACKEND_URL=http://localhost:8000

# Optional: set a fixed extension key so team members share one stable extension ID.
EXTENSION_KEY=

# Optional: when set, this overwrites extension/manifest.json oauth2.client_id at build time.
EXTENSION_OAUTH_CLIENT_ID=